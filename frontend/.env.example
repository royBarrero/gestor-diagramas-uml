# Copiar a frontend/.env para desarrollo local si querés apuntar a un backend
# distinto de localhost:8000. Para el build de Docker, esta variable se pasa
# como --build-arg VITE_API_URL (ver docker-compose.yml y DEPLOY.md).
VITE_API_URL=http://localhost:8000
